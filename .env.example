# SEC EDGAR requires a User-Agent; use your email (edgartools uses this).
EDGAR_EMAIL=your.name@example.com

# Directory for Parquet output (relative to project root or absolute path).
DATA_DIR=data/parquet

# Optional: edgartools cache dir (default: data/.edgar inside project — avoids ~/.edgar permission issues).
# EDGAR_LOCAL_DATA_DIR=data/.edgar

# Optional: Gemini API key for AI analysis in run_ticker.py (MD&A + common-size + flux → analysis).
# GEMINI_API_KEY=your_key_from_google_ai_studio
