# SEC EDGAR requires a User-Agent; use your email (edgartools uses this).
EDGAR_EMAIL=your.name@example.com

# Directory for Parquet output (relative to project root or absolute path).
DATA_DIR=data/parquet
